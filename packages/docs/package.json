{
  "name": "@tomato-mien/docs",
  "private": true,
  "scripts": {
    "dev": "npm run build && concurrently \"npx @tailwindcss/cli -i src/styles.css -o dist/styles.css --watch\" \"serve dist -l 3000\"",
    "screenshot-og": "node scripts/screenshot-og.js && magick assets/og-image.png -resize 1200x630 -strip -quality 90 assets/og-image.jpg && rm assets/og-image.png && cp assets/og-image.jpg dist/assets/og-image.jpg",
    "screenshot-previews": "node scripts/screenshot-previews.js",
    "build": "npx @tailwindcss/cli -i src/styles.css -o dist/styles.css --minify && node -e \"const fs=require('fs'),v=require('../../package.json').version;fs.writeFileSync('dist/index.html',fs.readFileSync('src/index.html','utf8').replaceAll('__APP_VERSION__',v))\" && cp src/privacy.html dist/privacy.html && cp src/support.html dist/support.html && cp src/sitemap.xml dist/sitemap.xml && cp src/robots.txt dist/robots.txt && cp src/llms.txt dist/llms.txt && rm -rf dist/assets && mkdir -p dist/assets && cp ../../assets/icon*.png ../../assets/icon.ico ../../assets/icon-rounded.png dist/assets/ && cp -r assets/screenshots dist/assets/screenshots && cp assets/og-image.jpg dist/assets/og-image.jpg"
  },
  "dependencies": {
    "@tomato-mien/design-tokens": "*"
  },
  "devDependencies": {
    "@tailwindcss/cli": "^4.1.14",
    "concurrently": "^9.2.1",
    "serve": "^14.2.4"
  }
}
